"use client";
import Link from "next/link";
import { useState } from "react";
import { LAB_ARTICLES, POPULAR_TAGS } from "@/lib/data";
export default function LabPage(){ const [tag,setTag]=useState('all'); const [cat,setCat]=useState('all'); const categories=Array.from(new Set(LAB_ARTICLES.map(a=>a.category))); const list=LAB_ARTICLES.filter(a=>(tag==='all'||a.tags.includes(tag))&&(cat==='all'||a.category===cat)); return <main className="max-w-4xl mx-auto p-4"><h1 className="text-xl font-bold mb-3">Lab記事一覧</h1><div className="flex flex-wrap gap-2 mb-2"><button onClick={()=>setCat('all')} className="text-xs border px-2 py-1 rounded">all</button>{categories.map(c=><button key={c} onClick={()=>setCat(c)} className="text-xs border px-2 py-1 rounded">{c}</button>)}</div><div className="flex flex-wrap gap-2 mb-3"><button onClick={()=>setTag('all')} className="text-xs border px-2 py-1 rounded">all</button>{POPULAR_TAGS.map(t=><button key={t} onClick={()=>setTag(t)} className="text-xs border px-2 py-1 rounded">{t}</button>)}</div>{list.map(a=><Link key={a.id} href={`/lab/${a.slug}/`} className="block border rounded p-3 mb-2"><p className="font-semibold">{a.titleJapanese}</p><p className="text-xs">{a.summaryJapanese}</p></Link>)}</main>}
